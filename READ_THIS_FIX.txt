================================================================================
CRITICAL ERROR FIX - READ IMMMEDIATELY
================================================================================

The error you see (`func sseError not found`) is coming from your WALLET EXTENSION (Phantom/Solflare).
IT IS NOT A BUG IN THE GAME CODE.

To demonstrate the project WITHOUT this error:

1. COPY THIS ADDRESS: http://localhost:3000

2. OPEN A "NEW INCOGNITO WINDOW" (Ctrl + Shift + N) in Chrome/Brave.

3. PASTE THE ADDRESS.

4. The game will run perfectly.

--------------------------------------------------------------------------------
REASON: Your browser extension is corrupted or conflicting with the Hot Reload.
INCOGNITO MODE disables the broken extension data.
--------------------------------------------------------------------------------
GO NOW.
================================================================================
